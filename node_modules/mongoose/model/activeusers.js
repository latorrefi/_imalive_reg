var mongoose = require('mongoose');

var db =mongoose.connect('mongodb://guest:guest@ds043714.mongolab.com:43714/_imalive_reg_');

var Schema = mongoose.Schema;
var ObjectId = Schema.Types.ObjectId;

var activeusersSchema = new Schema({
    _id:{ type:ObjectId, default: mongoose.Types.ObjectId },
    usernumber:Number,
    password:String,
    mail:String
});

//compile schema to model
module.exports = db.model('ActiveUsers', activeusersSchema,'activeusers');